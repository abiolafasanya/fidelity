<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="/">
      <span class="text-primary fw-bold">Fidelity Submittion Portal</span>
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navmenu"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navmenu">
      <ul class="navbar-nav ms-auto">
        <% if(loggedIn) { %> 
        <li class="nav-item">
          <% if(!data.isAdmin && !data.isTeacher) { %> <a class="nav-link" href="/dashboard">Home</a> <% } %> 
          <% if(data.isAdmin) { %> <a class="nav-link" href="/admin">Home</a> <% } %> 
          <% if(data.isTeacher) { %> <a class="nav-link" href="/teacher">Home</a> <% } %> 
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/assignment/results">Result</a>
        </li>
        <li class="nav-item d-flex">
          <form action="/logout?_method=DELETE" method="post">
              <button class="btn btn-primary mx-1" type="submit">LogOut</button>
          </form>
          <% } else { %>
          <form action="/register" method="get">
              <button class="btn btn-secondary mx-1" type="submit">Register</button>
          </form>
          <form action="/login" method="get">
              <button class="btn btn-primary mx-1" type="submit">Login</button>
          </form>
          <% } %>
  </div>
</nav>
