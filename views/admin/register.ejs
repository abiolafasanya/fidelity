<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="bg-light">
    <header><%- include('../partials/header'); %></header>

    <main>
      <div class="container">
        <div class="bg-light p-5 rounded">
          <h1 class="text-center">Registration</h1>
        </div>
        <div class="w-75 m-auto p-4 py-5">
          <form
            action="/admin/create-user"
            method="post"
            enctype="multipart/form-data"
          >
            <div class="input-group mb-3">
              <span class="input-group-text" id="">Username</span>
              <input
                type="text"
                class="form-control"
                name="username"
                placeholder="Enter Username"
                id="username"
              />
            </div>

            <div class="input-group mb-3">
              <label class="input-group-text" for="role">Class</label>
              <select class="form-select" id="role" name="role">
                <option selected>Select user type</option>
                <option value="user">user</option>
                <option value="teacher">teacher</option>
                <option value="admin">admin</option>
              </select>
            </div>

            <p>Gender</p>
            <div class="form-check form-check-inline">
              <input
                class="form-check-input"
                type="radio"
                name="gender"
                id="gender"
                value="male"
              />
              <label class="form-check-label" for="gender"> Male </label>
            </div>
            <div class="form-check form-check-inline">
              <input
                class="form-check-input"
                type="radio"
                name="gender"
                id="gender"
                value="female"
              />
              <label class="form-check-label" for="gender"> Female </label>
            </div>

            <div class="input-group mb-3">
              <span class="input-group-text" id="">Password</span>
              <input
                type="password"
                class="form-control"
                placeholder="Password"
                name="password"
              />
              <span class="input-group-text" id="">Confirm Password</span>
              <input
                type="password"
                class="form-control"
                placeholder="Confirm password"
                name="cpassword"
              />
            </div>

            <div class="input-group mb-3">
              <input type="file" name="photo" class="form-control" id="photo" />
              <label class="input-group-text" for="photo">Upload Picture</label>
            </div>
            <div class="form-group">
              <input type="submit" value="Submit" class="btn btn-primary" />
              <span class=""
                >Have an Account Already <a href="/user/login">Login</a></span
              >
            </div>
          </form>
        </div>
      </div>
    </main>

    <!-- <script>
      function edValueKeyPress() {
        let fname, lname, username;
        fname = document.getElementById("fname").value;
        lname = document.getElementById("lname").value;

        username = document.getElementById("username");
        username.value = `FDS/${fname
          .charAt(0)
          .toLowerCase()}${lname.toLowerCase()}`;
      }
    </script> -->

    <footer><%- include('../partials/foot'); %></footer>
  </body>
</html>
